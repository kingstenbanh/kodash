/**
 * Checks if value is less than other
 * @param value: The value to compare
 * @param other: The other value to compare
 * @returns {boolean}: Return true if value is less than other, else false
 */

function lt(value, other) {
    return value < other;
}

module.exports = lt;